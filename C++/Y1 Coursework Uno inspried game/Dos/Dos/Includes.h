#include <iostream> // Allows data to be displayed and taken from the console
#include <string> // Adds string data type
#include <vector> // Adds vector data structure
#include <stack> // Adds stack data structure
#include <conio.h> // Adds getch function which allows single characters to be collected from player input, used to halt program until a key is pressed
#include <exception> // Allows exception handling
#include <cctype> // Allows the case of letters to be changed
using namespace::std;

